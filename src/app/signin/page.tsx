import React from "react";
import Logo from "../components/Logo";

import Signincomp from "../components/Sign-in-out/Signincomp";

export const metadata = {
  title: "Sign in",
};

export default function Signin() {
  return <Signincomp />;
}
